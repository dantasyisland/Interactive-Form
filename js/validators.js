const isNameValid = (name) => {
    const nameReg = /^[a-z ,.'-]+$/i;
    return nameReg.test(name);
}
const isEmailValid = (email) => {
    const emailReg = /^[^@]+@[^@.]+\.[a-z]+$/i;
    return emailReg.test(email);
}
const isActiviesValid = (name) => {
    const nameReg = /^[a-z ,.'-]+$/i;
    return nameReg.test(name);
}
const isNameValid = (name) => {
    const nameReg = /^[a-z ,.'-]+$/i;
    return nameReg.test(name);
}




const isNameValid = (input) => {
    const regEx =
    return regEx.test(input)
}